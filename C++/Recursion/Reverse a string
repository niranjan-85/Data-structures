
#include<iostream>
#include<stdlib.h>
#include<string.h>
typedef int I;
int recurse(char a[], I l) {
	if (l>=0)
	{
		std::cout << a[l];
		return recurse(a, l-1);

	}
}
void main() {
	char a[20];
	I length_;
	fgets(a, sizeof(a), stdin);
	length_ = strlen(a)-2;
	char k= recurse(a, length_);
}
