#include<iostream>
#include<stdlib.h>
using namespace std;
typedef int I;
typedef struct node {
	I data;
	node* next;
};
node* head,*newnode,*temp;
void main() {
	I n=3;
	head = NULL;
	while (n != 0) {

		newnode = (node*)malloc(sizeof(node));
		cout << "Enter data";
		cin >> newnode->data;
		if (head == NULL) {
			temp = head = newnode;
		}
		else
		{
			temp->next = newnode;
			temp = newnode;
		}
		n = n - 1;
		if (n == 0) {
			temp->next = NULL;
		}
	}
	temp = head;
	for (I i = 0; i < 3; i++) {
		cout << temp->data<<endl;
		cout << temp->next << endl;;
		temp = temp->next;
	}


}
