#array rotation

import array as arr

d=int(input("enter the number of rotations"))

a=arr.array('i',[int(input()) for x in range(5)])
swap=a[0]
j=0
for f in range(d):
    for x in range(1,len(a),1):
        a[x],a[j]=a[j],a[x]
        j+=1
    j=0
    x=0
    print(a)
