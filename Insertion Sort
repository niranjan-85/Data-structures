import numpy as np

a=np.array([6,5,4,3,2,1])
                                   cost  time
for i in range(1,len(a)):           c1    n                        ##to traverse in unsorted array.
    key=a[i]                        c2    n-1
    j=i-1                           c3    n-1
    while j>=0 and a[j]>key:        c4    sum(Tj)                  ##always compare a single value with every element in the sorted array
                                                                      and to traverse in the sorted array.
        a[j+1]=a[j]                 c5    
        j-=1
    a[j+1]=key                                                     ##place the valid number in the array (last pos.)
print(a)


#Best case complexity: O(n)
#Worst case complexity: O(n^2)



