#** to check if numbers are distinct in a sequence #**



n=int(input("enter range "))
l=[]
for x in range(n):
    l.append(int(input()))

length_list=len(l)

c=set(l)
final=list(c)
length_set=len(final)

if length_list-length_set==0:
    print("all numbers are distinct")
else:
    print("all numbers are not distinct")
